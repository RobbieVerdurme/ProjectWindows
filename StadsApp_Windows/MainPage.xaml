<Page
    x:Class="StadsApp_Windows.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StadsApp_Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <SplitView x:Name="mySplitView" CompactPaneLength="50" IsPaneOpen="True" DisplayMode="CompactInline">
        <SplitView.Pane>
            <StackPanel Background="Gray">
                <Button FontFamily="Segoe MDL2 assets"
                        Content="&#xE700;"
                        Click="Button_Click"
                        Width="50"
                        Height="50"/>
                <StackPanel Orientation="Horizontal" Tapped="StackPanel_Tapped">
                    <Button FontFamily="Segoe MDL2 Assets"
                            Content="&#xE825;"
                            Width="50"
                            Height="50"/>
                    <TextBlock Text="Overzicht Ondernemingen"
                               FontSize="18"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </SplitView.Pane>
        <SplitView.Content>
            <Frame x:Name="mainFrame"/>
        </SplitView.Content>
    </SplitView>
</Page>
